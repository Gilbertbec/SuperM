@model List<SuperM.Data.Entities.ShoppingCart>

@{
	ViewBag.Title = "CartList";
}

<h2>CartList</h2>
@foreach (var item in Model)
{

	@*@Html.Partial("CartListDetails",item)*@
	@Html.EditorFor(x => item.Product.Name);
	@Html.DisplayFor(x => item.Product.SellingPrice);
	@Html.DisplayName(item.Product.SellingPrice.ToString());
	@Html.Display(item.Product.SellingPrice.ToString());

	@Html.DisplayFor(x => item.QuantityToBuy);
	@Html.DisplayName(item.QuantityToBuy.ToString());
	@Html.Display(item.QuantityToBuy.ToString());
}

@*@using (Html.BeginForm())
	{
		@Html.AntiForgeryToken()

		<div class="form-horizontal">
			<h4>CartList</h4>
			<hr />
			@Html.ValidationSummary(true, "", new { @class = "text-danger" })
			@Html.HiddenFor(model => model.CartListId)

			<div class="form-group">
				<div class="col-md-offset-2 col-md-10">
					<input type="submit" value="Save" class="btn btn-default" />
				</div>
			</div>
		</div>
	}*@

<div>
	@Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
	@Scripts.Render("~/bundles/jqueryval")
}
